{
  "name": "widgets-shell",
  "version": "0.1.0",
  "private": true,
  "description": "Electron widget shell with tray, persistent widget windows, pinning, and autostart.",
  "main": "src/main/main.js",
  "scripts": {
    "build:media-session": "scripts\\build-windows-media-session.cmd",
    "start": "electron .",
    "test": "node --test"
  },
  "devDependencies": {
    "electron": "^42.4.1"
  },
  "dependencies": {
    "chokidar": "^5.0.0"
  }
}
